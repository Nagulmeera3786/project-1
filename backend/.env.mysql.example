# MySQL configuration for ABC project
# Copy to backend/.env and update values.

DB_ENGINE=django.db.backends.mysql
DB_NAME=abc_sms
DB_USER=root
DB_PASSWORD=change-me
DB_HOST=127.0.0.1
DB_PORT=3306

# Optional connection tuning
# DB_CONN_MAX_AGE=60
# DB_CHARSET=utf8mb4
